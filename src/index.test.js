/*global describe, it, beforeEach, afterEach */
import assert from 'chai';
import * as sinon from 'sinon';

/*describe('index', () => {

    beforeEach(() => {
    });

    afterEach(() => {
    });

    describe('', () => {

        it('', (done) => {
            done();
        });

    });

});*/
